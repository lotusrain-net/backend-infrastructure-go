# Compatibility

The `0.1.x` line preserves documented exports, props, JSON fields, and CSS
tokens. Removing an export, changing a signature or prop, changing a wire
field, or removing a token requires `0.2.0` or later. Additive APIs may be
introduced in a minor release.
